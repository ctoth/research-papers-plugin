# Code Review Fixes - 2026-03-21

## GOAL
Implement 5 fixes from code review, in dependency order.

## DONE
- **Fix 1** (COMMITTED 58e121a): Added mechanism/comparison/limitation claim types to extract-claims SKILL.md
  - Updated Claim Value Filter with adjudication filter
  - Added Steps 2.7-2.9 with examples from real propstore papers (deKleer, Clark, Dung)
  - Updated quality checklist and output summary
- **Fix 2** (COMMITTED fcb6920): Added argumentation sections to paper-reader SKILL.md
  - "Arguments Against Prior Work" and "Design Rationale" between Limitations and Testable Properties
  - All bullets have *(p.N)* citation placeholders
- **Fix 3 + Fix 5** (EDITED, NOT YET COMMITTED): generate_claims.py changes
  - Added `_resolve_page_reference()` function - scans 500-char window around target for `(p.\d+)` patterns
  - Wired `parse_equations()` and `parse_testable_properties()` into `generate_claims()` (Fix 5)
  - Added page resolution pass at end of `generate_claims()` for all claim types
  - These two fixes combined because they both modify generate_claims() and the page resolution applies to the new equation/observation claims too

## REMAINING
- **Fix 4**: Add decomposition rule for definitional claims to extract-claims SKILL.md
  - Goes in the "Claim Decomposition" section after the split signals
  - Prevents claim 3/5/6 redundancy pattern (seen in Clark_2014 where model definition split across multiple claims)
- Need to run tests after committing Fix 3+5
- Need to commit Fix 3+5

## FILES MODIFIED
- `plugins/research-papers/skills/extract-claims/SKILL.md` - Fix 1 (committed), Fix 4 (pending)
- `plugins/research-papers/skills/paper-reader/SKILL.md` - Fix 2 (committed)
- `plugins/research-papers/scripts/generate_claims.py` - Fix 3+5 (edited, uncommitted)

## BRANCH
`fix/code-review-claim-types` off master

## OBSERVATIONS
- The existing test `test_parameter_table_row_count` expects exactly 3 parameter claims from SAMPLE_NOTES_MD
- SAMPLE_NOTES_MD contains 1 equation ($$...$$) and 2 testable properties
- After Fix 5, generate_claims will also produce equation + observation claims, so total claim count increases
- The test checks `param_claims` specifically (filtered by type=="parameter") so it should still pass
- But `test_full_pipeline_generates_valid_yaml` just checks `len > 0` — should be fine
- Schema validation test may need attention if equation/observation claims don't match schema
